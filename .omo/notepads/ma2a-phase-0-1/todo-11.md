# Todo 11

- Implemented schema v2 credential epochs and digest-only revocable sessions.
- Implemented bounded Argon2id derivation and strict verifier-shape validation.
- Implemented dual-stack loopback Axum serving, Host/peer/origin/fetch-site guards, CSRF, strict cookies, headers, limits, and deterministic expiry.
- Implemented CLI-only password set/reset with no-echo confirmation through an injected typed current-user control-client boundary. The app has no direct store dependency or `MA2A_STATE_DIR` path.
- Added `docs/web-security.md` and integration coverage for browser setup absence, reset-wide revocation, token secrecy, idle/absolute expiry, strict headers/cookies, malformed inputs, limits, non-loopback denial, and shared-port IPv6.
- Hardened duplicate Host, Origin, Sec-Fetch-Site, cookie-header, and session-cookie handling to fail closed.
- Verification: workspace format and strict Clippy clean; workspace build succeeded; 79 nextest tests and 41 Web tests passed. CLI help and unavailable-transport paths were exercised in a terminal; the production Web bundle rendered in Playwright without console warnings.
- Independent review fixed the browser's discarded-CSRF/logout flow and verified login/touch/logout against the production binary with no browser storage writes.
- Independent review verdict: REJECT. `RuntimeControlClient` is a typed in-process adapter, not authenticated current-user IPC; the CLI directly opens `CurrentUserRuntime` and mutates the same store in its own process.
- Remediated the rejection: the daemon now owns `CurrentUserRuntime`, dispatches typed UI credential/session commands through owner-private local IPC with replay handling, and the CLI autostarts then calls `LocalApiClient` instead of opening runtime state in-process.
- Final verification: `scripts/check.sh`, focused Rust tests, strict Clippy, 42 Web tests, and live revoke/status/error/shutdown CLI QA passed. All changed Rust files satisfy the 250 pure-LOC policy.
- Independent remediation review fixed three blocking defects: wrong-CSRF requests no longer touch session expiry, daemon UI mutations now report/adopt the persisted authoritative revision and password state, and browser reload/logout now restores then expires a separate host-only strict CSRF cookie without exposing the bearer.
- Added regressions at Store, HTTP-to-SQLite, daemon IPC, cookie-header, and Web-client boundaries. The exact committed head `323dd585929c3f4b118ccedf0d06687180a68d67` passes workspace format, strict Clippy, workspace build, 141/141 quiet nextest tests, 43/43 Web tests, Web production build, Rust no-excuse checks, tracked/staged secret scans, and gitignore coverage.
- Live production-binary Playwright QA passed: login wrote only the readable CSRF cookie and no local/session storage, invalid CSRF returned 403 without revocation, hard navigation retained an enabled logout action, and logout returned to `/login` with `document.cookie` empty. Final independent verdict: ACCEPT.
- 2026-08-29T23:33:44+10:00: Corrected the Settings password-recovery command to `ma2a ui password reset` while preserving `/setup` instructions for both initial `password set` and recovery `password reset`. TDD evidence: the focused Settings route regression failed on the old command, then passed after the one-token correction; the full Web sequence passed 44 tests and production build, and Playwright verified both rendered routes with zero console warnings/errors. The locked aggregate gate remains blocked by the pre-existing untouched `crates/ma2a-runtime/tests/web_security.rs` at 275 pure LOC against the 250-line policy.
- 2026-08-30T00:07:11+10:00: Split `request_body_deadline_and_rate_state_are_bounded` intact from `web_security.rs` into focused `web_limits.rs`, leaving request-hardening coverage unchanged while bringing both integration-test crates below the 250 pure-LOC cap. Verification passed: focused Web security/limits tests 5/5, full `ma2a-runtime` tests 45/45, `cargo fmt --all -- --check`, `cargo run --locked -p xtask -- check-loc`, and the complete locked aggregate `xtask check` including strict workspace Clippy, all workspace tests, dependency checks, Biome, TypeScript, and 44/44 Web tests. LSP diagnostics were attempted for both files but the isolated worktree is outside the request cwd.
